## Context: context-compile-inputs-ctx
| Key | Value |
| --- | ----- |
| nuenv-input | github:xav-ie/nuenv — Nushell-based derivation builder, via lib.mkDefault |
| promptyst-input | github:blessuselessk/promptyst — Typst DSL for structured prompts, flake=false, via lib.mkDefault |
| pattern | Takes `lib` argument; both URLs use mkDefault so downstream can override |