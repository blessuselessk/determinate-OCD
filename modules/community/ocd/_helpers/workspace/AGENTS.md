# AGENTS.md

<!-- Immutable workspace definitions live in _system/ and are provisioned by
     the NixOS flake on every rebuild. They are permanent, long-lived settings.
     This mutable workspace file imports what it needs from _system/ and can be
     freely extended by the agent. -->

@[system-agents](./_system/AGENTS.md)
